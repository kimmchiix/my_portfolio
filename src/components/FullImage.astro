---
const { src, alt = "", className = "" } = Astro.props;
---

<div
  class={`fade-up group flex justify-center items-center h-[40vh] md:h-[70vh] rounded-xl ${className}`}
>
  <img
    src={src}
    alt={alt}
    loading="lazy"
    class="w-auto max-h-full object-contain rounded-xl transition-transform duration-500 ease-out group-hover:scale-105"
  />
</div>

<style>
/* Smooth lift for container if you want (optional) */
.group:hover {
  transform: translateY(-4px);
  transition: transform .35s ease;
}
</style>
